Revise the article for readability. Apply the local edits in the review notes, or leave a passage when the marked style is doing work.

Request: {requestLabel}

{researchToolHint}

Reader calibration:
{readerContext}

The current article is provided with this prompt.

{readabilityHints}

Revise the smallest unit that fixes the processing cost: unnest clauses, split when there is more than one move, restore canonical order, use the simpler word when it carries the same meaning, or drop performative formality. Do not add recaps, checklists, or a global style pass. Length-neutral by default.

Return a clean, standalone markdown article. Do not mention this review.
