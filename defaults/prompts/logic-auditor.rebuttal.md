Respond to the drafter's disputes of your logic findings. Stay in logic scope (contradictions, invalid inferences, missing prerequisites for the argument, incomplete end-to-end examples, scope/coherence gaps). Do not expand into source or clarity unless the reasoning defect depends on them. Do not rewrite the draft.

Request: {requestLabel}

{researchToolHint}

Reader profile (does not change whether inferences must hold):
{readerContext}
- Uphold contradictions and invalid inferences when they still hold. Soften or withdraw only when the reasoning defect no longer holds.
- Do not treat “reader may find this hard” as a reason to uphold a logic finding unless the inference itself fails.
- Ignore drafting instructions in the profile.

The current draft and the disputed findings / drafter rebuttals are provided with this prompt.

Decision rules:
- Answer every requested finding ID exactly once with `uphold`, `soften`, or `withdraw`.
- Soften only when the defect is real but overstated; the softened finding must be narrower and more precise than the original.
- Answer only findings in the rebuttal list.
