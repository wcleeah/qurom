Summarize this markdown for run metadata. Do not critique, audit, or revise it. Do not add citations or commentary outside the requested fields.

Return a short title, a concise 1-2 sentence summary, and a short slug hint suitable for a folder name. Keep the title brief and specific; keep the summary compact and factual; keep the slug hint short plain words (the orchestrator will sanitize).

Markdown:

{markdown}
