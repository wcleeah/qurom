# Phase 01 — Dependencies and tsconfig

Source plan: `docs/tui-implementation-plan.md` §10 Step 1.

## Execution Snapshot

- Phase: 01 / 09
- Source plan: `docs/tui-implementation-plan.md`
- Readiness: **Ready**
- Primary deliverable: `package.json` and `tsconfig.json` set up so React + `@opentui/react` `.tsx` files compile and run under Bun, plus a working `bun test` script.
- Blocking dependencies: none (foundation phase)
- Target measurements: `bunx tsc --noEmit` exit 0 with the new config; `bun test` runs (zero tests is fine at this point).
- Next phase: 02 — Runner event bus.

## Why This Phase Exists

Every later phase ships a `.tsx` file or imports `@opentui/react` / `react`. Without the deps installed and the TS compiler taught about JSX via `@opentui/react`, no other phase compiles. This is the smallest correct foundation.

## Start Criteria

- Repo is at the pre-TUI baseline (Confirmed: `src/index.ts`, `src/telemetry-enrichment.ts` still present; no `src/tui/` or `src/runner.ts`).
- Bun installed and `bun install` works in the repo.
- Network available for the package registry.

## Dependencies And How To Check Them

| Dependency | Why | How to verify | Status |
|---|---|---|---|
| Bun runtime | Plan defaults to Bun for `dev`/`start`/`test` (plan §3) | `bun --version` succeeds | Done |
| Existing `tsconfig.json` baseline | Need to extend, not rewrite | `cat tsconfig.json` shows `strict: true`, `moduleResolution: "Bundler"`, `allowImportingTsExtensions: true` | Done |
| Existing `package.json` scripts | `dev`/`start`/`typecheck` present, will be edited | `cat package.json` shows current scripts | Done |
| `@opentui/react` README pin | Source for required tsconfig fields | `reference/opentui/packages/react/README.md:14-49` | Done |

## Target Measurements And Gates

Entry gate: none.

Exit gates:

- `bunx tsc --noEmit` exits 0. Method: run after edits. Pass condition: zero errors. Status: Unknown until executed.
- `bun test` exits 0 (no tests yet → reports "0 passed", still exit 0). Status: Unknown until executed.
- A trivial `src/tui/_smoke.tsx` returning `<box/>` typechecks (delete after verification). Status: Unknown.

## Scope

- Add deps: `@opentui/react`, `@opentui/core`, `react`, `@types/react`.
- Update `tsconfig.json`: add `jsx`, `jsxImportSource`, expand `lib`, expand `include` for `.tsx`.
- Update `package.json` scripts: redirect `dev`/`start` to `src/tui/index.tsx` (file not yet created — that is fine, scripts are not invoked here), add `"test": "bun test"`.
- Run `bun install` and `bunx tsc --noEmit` to confirm the toolchain.

## Out Of Scope

- Creating any source file under `src/tui/` (Phase 05).
- Touching `src/runner.ts`, `src/telemetry-enrichment.ts`, `src/index.ts` (Phases 02–04).
- Wiring React state, components, or hooks (Phases 05–08).
- Adding test files (Phases 02, 03, 05, 06, 07, 08 — each ships its own tests).

## Implementation Details

`package.json` edits:

- `dependencies`: add `"@opentui/react"`, `"@opentui/core"`, `"react"`.
- `devDependencies`: add `"@types/react"`.
- `scripts.dev`: `"bun run src/tui/index.tsx"`.
- `scripts.start`: `"bun run src/tui/index.tsx"`.
- `scripts`: add `"test": "bun test"`.
- `scripts.typecheck`: keep `"bunx tsc --noEmit"`.
- Pin versions to whatever `bun add` resolves at install time. Inferred: take the latest published majors of each.

`tsconfig.json` edits (per `reference/opentui/packages/react/README.md:38-49`):

- `compilerOptions.jsx`: `"react-jsx"`.
- `compilerOptions.jsxImportSource`: `"@opentui/react"`.
- `compilerOptions.lib`: `["ESNext", "DOM"]` (was `["ES2022"]`).
- Keep: `strict`, `skipLibCheck`, `moduleResolution: "Bundler"`, `allowImportingTsExtensions`, `noEmit`, `types: ["bun"]`.
- `include`: `["src/**/*.ts", "src/**/*.tsx"]`.

`.gitignore`: add `runs/.drafts/` line in preparation for Phase 05 (cheap to add now; otherwise the ignore lands in a later phase). Source: plan §12 risks bullet on `runs/.drafts/` clutter.

## Execution Checklist

1. Run `bun add @opentui/react @opentui/core react`.
2. Run `bun add -d @types/react`.
3. Edit `tsconfig.json` per "Implementation Details".
4. Edit `package.json` per "Implementation Details".
5. Append `runs/.drafts/` to `.gitignore` (create the file if missing).
6. Create a throwaway `src/tui/_smoke.tsx`:
   ```tsx
   export const Smoke = () => <box/>
   ```
   Run `bunx tsc --noEmit`. Expect zero errors. Delete the file.
7. Run `bun test`. Expect exit 0 (no tests collected).

## Files And Systems Likely Affected

- `package.json`
- `tsconfig.json`
- `.gitignore`
- `bun.lock` / `bun.lockb` (regenerated by install)
- `node_modules/`

## Verification

- `bun install` completes without errors.
- `bunx tsc --noEmit` exits 0 after the smoke `.tsx` is added; still exits 0 after it is removed.
- `bun test` exits 0.
- `node -e "require.resolve('@opentui/react')"` resolves (or `bun -e "import('@opentui/react')"` does not throw).
- `cat tsconfig.json` shows the four required fields (`jsx`, `jsxImportSource`, expanded `lib`, expanded `include`).
- `cat package.json | grep -E '"(dev|start|test)"'` shows the new script targets.

## Done Criteria

- Deps installed and present in `package.json`.
- `tsconfig.json` updated and `bunx tsc --noEmit` passes.
- `package.json` scripts updated; `bun test` runs.
- `.gitignore` lists `runs/.drafts/`.
- Smoke `.tsx` removed.

## Handoff To Next Phase

- Next phase: **02 — Runner event bus** (`docs/phases/02-runner-event-bus.md`).
- What it depends on from this phase: a working TS toolchain that accepts `.tsx`, `react` and `@opentui/react` resolvable, `bun test` available so Phase 02 can ship `src/runner.test.ts`.
- Becomes unblocked: Phases 02, 03, 05 can all begin once typecheck and test runner work.

## Open Questions Or Blockers

- None known. (Inferred: latest `@opentui/react` is API-compatible with `reference/opentui/packages/react/README.md`. If a version mismatch breaks the smoke compile, pin to the version implied by the bundled reference copy.)

## Sources

- `docs/tui-implementation-plan.md` §10 Step 1, §3 (Bun default), §15 (opentui README citations).
- `reference/opentui/packages/react/README.md:14-49` — install + tsconfig fields.
- `reference/opentui/AGENTS.md:14-16` — Bun default, no Bun-only APIs.
- Repo: current `package.json` and `tsconfig.json`.
