Plan the deep dive before writing full prose.

Return only JSON that matches the requested schema.

Planning rules:
- Make the throughline explicit.
- Keep the number of main sections small and necessary.
- Each section should answer one concrete question that must be resolved for the finish line to hold.
- Keep key points short and actionable.
- Prefer a running example that can carry through the whole document.
- Surface the exact places where a gap-sensitive reader would challenge the draft next.
- List the prerequisite terms or mechanisms that must be explained before the draft can rely on them.
- If sibling mechanisms must be mentioned to keep the explanation true, include them explicitly in the plan instead of hiding them as caveats.
